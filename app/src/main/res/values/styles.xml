<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/PLight</item>
        <item name="colorPrimaryDark">@color/PRegular</item>
        <item name="colorAccent">@color/SRegular</item>
    </style>

</resources>
